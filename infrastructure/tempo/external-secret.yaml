---
# =============================================================================
# Tempo MinIO Credentials — ExternalSecret
# =============================================================================
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: tempo-minio-credentials
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: tempo-minio-credentials
    creationPolicy: Owner
  data:
    - secretKey: access-key
      remoteRef:
        key: tempo/minio
        property: access-key
    - secretKey: secret-key
      remoteRef:
        key: tempo/minio
        property: secret-key
