# This file is no longer needed - the Helm chart handles all deployments
# See argocd/apps/freshrss-app.yaml for Helm chart configuration
#
# The following resources are now managed by the Helm chart:
# - Deployment
# - Service
# - PersistentVolumeClaim
# - SecurityContext
#
# Only infrastructure/freshrss/httproute.yaml is needed for Envoy Gateway ingress
